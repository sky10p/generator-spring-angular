{
    "extends": "../tslint.json",
    "rules": {
        "directive-selector": [
            true,
            "attribute",
            "<%= prefix_angular %>",
            "camelCase"
        ],
        "component-selector": [
            true,
            "element",
            "<%= prefix_angular %>",
            "kebab-case"
        ]
    }
}
